export EDITOR='vim'

alias server="browser-sync start --server --files ./"

export NVM_DIR="~/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm

export GH_TOKEN="" # get this from github
export NPM_TOKEN="" # get this from npm
